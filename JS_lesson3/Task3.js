// Task3**. Напишите программу, которая принимает целое положительное число n (одно любое число от 1 до 9), и выводит сумму равную 
// n + nn + nnn, где n не перемножаются, а конкатенируются
const n = 5;
const concatination = n.toString();
const firstConcatination = concatination + concatination;
const secondConcatination = firstConcatination + N.toString();
const result = n + Number(firstConcatination) + Number(secondConcatination);
console.log(result);

 